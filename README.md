Парсер лога формата %h - - %t "%r" %s %b "%{Referer}" "%{User-Agent}" %d

запуск: python log_parser.py <директория или файл>
доступны опции:
--output <директория или файл куда сохранять результирующие json>
--pattern <regex для выбора файлов логов из директории (по умолчанию *.log)>
